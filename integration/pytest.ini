[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
bdd_features_base_dir = features
markers =
    integration: real-AWS integration tests (require credentials + cdk deploy)
addopts =
    -v
    --strict-markers
    --tb=short
